---
phase: 05-seo-infrastructure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/authors/redaktion.jpg
  - src/content/authors/redaktion.json
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Author page displays a visible author image (not broken/hidden)"
    - "Author JSON-LD schema references valid image URL"
  artifacts:
    - path: "public/images/authors/redaktion.jpg"
      provides: "Author avatar image file"
      min_size: 1000  # bytes - actual image, not placeholder text
  key_links:
    - from: "src/content/authors/redaktion.json"
      to: "public/images/authors/redaktion.jpg"
      via: "image field path"
      pattern: "/images/authors/redaktion.jpg"
---

<objective>
Close verification gap: Add author photo image file so the author page displays a visible image instead of relying on the onerror fallback that hides broken images.

Purpose: Complete SEO-05 requirement "Author page exists with bio, credentials, and photo" which is currently blocked due to missing image file.

Output: Author photo file exists and displays on /autor/redaktion/ page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-infrastructure/05-VERIFICATION.md

# Key files
@bambini-fussball/src/content/authors/redaktion.json
@bambini-fussball/src/pages/autor/[id].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create professional team avatar SVG and save as author image</name>
  <files>public/images/authors/redaktion.jpg</files>
  <action>
Create directory structure and add a professional team/editorial avatar image for the "Redaktion Bambini-Fussball" author entry.

Since this is an editorial team (not a real person), create a professional placeholder:

1. Create directory: `bambini-fussball/public/images/authors/`

2. Create an SVG-based avatar converted to JPG format OR create a simple colored avatar with initials.

Option A (Preferred - Simple SVG converted approach):
- Use a data URI or inline SVG approach that Astro can serve
- Create a professional green (brand color) circle with "R" or team icon
- Background: #16a34a (green-600 from Tailwind - matches site theme)
- Size: 256x256 pixels minimum

Option B (If JPG required):
- Create a simple solid color image with CSS-style initials overlay
- Or download a CC0/public domain team avatar icon

For this editorial team context, a clean branded placeholder is MORE professional than a stock photo of random people.

The image path must be exactly: `/images/authors/redaktion.jpg`
(which maps to `public/images/authors/redaktion.jpg` in Astro)

Note: The current author page at src/pages/autor/[id].astro line 43 has `onerror="this.style.display='none'"` which hides broken images. Once a real image exists, this fallback won't trigger.
  </action>
  <verify>
1. File exists: `ls bambini-fussball/public/images/authors/redaktion.jpg`
2. File is an actual image (not empty): Check file size > 0 bytes
3. Dev server test: `npm run dev` then visit http://localhost:4321/autor/redaktion/ - image should be visible
  </verify>
  <done>
Author image file exists at public/images/authors/redaktion.jpg and displays on the author profile page without triggering the onerror fallback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify author schema references valid image URL</name>
  <files>src/content/authors/redaktion.json</files>
  <action>
Verify the author JSON still has the correct image path. The current content is:

```json
{
  "name": "Redaktion",
  "bio": "Die Redaktion von Bambini-Fussball.de besteht aus erfahrenen Trainern und Padagogen mit langjahrigem Hintergrund im Kinderfussball.",
  "credentials": "DFB-Lizenztrainer, Sportpadagogen",
  "image": "/images/authors/redaktion.jpg",
  "sameAs": []
}
```

The image path `/images/authors/redaktion.jpg` is correct (maps to public/images/authors/redaktion.jpg).

No changes needed unless Task 1 used a different filename. If the filename matches, no edit required.

Confirm the ProfilePage JSON-LD schema at /autor/redaktion/ will include the full image URL:
`https://bambini-fussball.pages.dev/images/authors/redaktion.jpg`
  </action>
  <verify>
1. Build succeeds: `cd bambini-fussball && npm run build`
2. Inspect generated JSON-LD in dist/autor/redaktion/index.html - should contain valid image URL
  </verify>
  <done>
Author schema image field points to existing image file. Build succeeds with no warnings about missing assets.
  </done>
</task>

</tasks>

<verification>
After completing all tasks, verify the gap is closed:

1. **File exists check:**
   ```bash
   ls -la bambini-fussball/public/images/authors/redaktion.jpg
   ```
   Expected: File exists with size > 0

2. **Build verification:**
   ```bash
   cd bambini-fussball && npm run build
   ```
   Expected: Build succeeds without image-related warnings

3. **Visual verification:**
   ```bash
   cd bambini-fussball && npm run dev
   # Visit http://localhost:4321/autor/redaktion/
   ```
   Expected: Author image displays in the 128x128 circular container

4. **JSON-LD verification:**
   Open page source of /autor/redaktion/ and find the ld+json script.
   Expected: `"image": "https://bambini-fussball.pages.dev/images/authors/redaktion.jpg"`
</verification>

<success_criteria>
- [ ] Image file exists at public/images/authors/redaktion.jpg
- [ ] Image is visible on /autor/redaktion/ page (not hidden by onerror)
- [ ] Build succeeds without warnings
- [ ] JSON-LD ProfilePage schema includes valid image URL
- [ ] Gap from 05-VERIFICATION.md is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-infrastructure/05-04-SUMMARY.md` using the summary template.

Update STATE.md pending todos to remove:
- [ ] Create /images/authors/redaktion.jpg for author profile image
</output>
