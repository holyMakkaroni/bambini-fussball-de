---
phase: 11-launch-preparation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bambini-fussball/public/images/og-default.jpg
autonomous: true

must_haves:
  truths:
    - "Default OG image exists for social sharing fallback"
    - "OG image has correct dimensions for social platforms (1200x630)"
    - "OG image represents site brand/topic"
  artifacts:
    - path: "bambini-fussball/public/images/og-default.jpg"
      provides: "Social sharing fallback image"
      min_lines: 0
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "/images/og-default.jpg"
      via: "og:image meta tag fallback"
      pattern: "og:image.*og-default"
---

<objective>
Create default Open Graph image for social sharing fallback.

Purpose: Ensure all pages have a visual representation when shared on social media (Facebook, Twitter, LinkedIn).
Output: Brand-appropriate 1200x630 JPG image for OG meta tags.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Check if BaseLayout already has og:image fallback
@bambini-fussball/src/layouts/BaseLayout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate default OG image using sharp</name>
  <files>bambini-fussball/public/images/og-default.jpg</files>
  <action>
Use Node.js with sharp to create a branded OG image:

1. Create a temporary script or run inline Node.js
2. Generate 1200x630 image with:
   - Green gradient background (brand colors #16a34a to #15803d - same as author avatar)
   - White text "Bambini-Fussball" centered
   - Subtitle "Dein Ratgeber fur den Kinderfussball" below

If sharp text rendering is too complex, create a simple gradient-only image:
   - Use SVG-to-JPG conversion (same pattern as 05-04 author avatar)
   - Green gradient fill
   - Text overlay via SVG

Alternative approach if sharp unavailable:
   - Create solid green (#16a34a) 1200x630 rectangle
   - Save as JPG with quality 85

The key requirement is having ANY image at the correct path and dimensions. A simple branded color is better than nothing.

Pattern reference (from Phase 05-04 author avatar):
```javascript
const sharp = require('sharp');
const svgBuffer = Buffer.from(`<svg>...</svg>`);
await sharp(svgBuffer).jpeg({ quality: 85 }).toFile(outputPath);
```
  </action>
  <verify>
Check file exists: `ls -la bambini-fussball/public/images/og-default.jpg`
Verify dimensions with: `file bambini-fussball/public/images/og-default.jpg` or check file size is reasonable (>10KB)
  </verify>
  <done>og-default.jpg exists at public/images/ with ~1200x630 dimensions</done>
</task>

<task type="auto">
  <name>Task 2: Verify BaseLayout og:image fallback wiring</name>
  <files>bambini-fussball/src/layouts/BaseLayout.astro</files>
  <action>
Check if BaseLayout.astro already has og:image meta tag with fallback.

If og:image exists but uses hardcoded path or different fallback:
- Update to use /images/og-default.jpg as fallback
- Ensure pages can override via props or slot

If og:image does NOT exist:
- Add og:image meta tag in head section
- Pattern:
  ```astro
  <meta property="og:image" content={ogImage ?? `${Astro.site}images/og-default.jpg`} />
  ```

Check og:image:width and og:image:height are also set (1200, 630).

DO NOT modify if already correctly configured - just verify.
  </action>
  <verify>
Grep BaseLayout.astro for og:image to confirm meta tag exists.
Run build and check a sample HTML output contains og:image meta tag.
  </verify>
  <done>og:image meta tag in BaseLayout falls back to /images/og-default.jpg</done>
</task>

</tasks>

<verification>
1. File exists: public/images/og-default.jpg
2. File size is reasonable (10KB - 200KB for a branded image)
3. Build succeeds without errors
4. Sample page HTML contains og:image pointing to production URL
</verification>

<success_criteria>
- Default OG image exists at /images/og-default.jpg
- Image is social-platform compatible (ideally 1200x630)
- Pending todo "Create /images/og-default.jpg" can be marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/11-launch-preparation/11-02-SUMMARY.md`
</output>
